.env
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.DS_Store

# Database files are generated by `make migrate` + `make seed`; never committed.
kivi.db
kivi.db-wal
kivi.db-shm
*.db
*.db-wal
*.db-shm

# NOTE: eval/results/ IS committed on purpose — the brief requires generated
# evaluation results to be present in the repository.
